import React, { Component, createContext } from 'react';

const UserContext = createContext('unknown');

class Component357 extends Component {
  render() {
    const { code } = this.props;
    return (
      <div>
        <pre>{code}</pre>
      </div>
    );
  }
}

export default Component357;
